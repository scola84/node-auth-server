{
  "name": "@scola/auth-server",
  "version": "2.0.0",
  "description": "Authorization (server)",
  "repository": "github:scola84/node-auth-server",
  "author": "M.C. van den Boogaart <scola@kvdb.name>",
  "license": "MIT",
  "main": "./dist/auth-server.js",
  "jsnext:main": "index.js",
  "dependencies": {
    "@scola/api-model": "2.0.1",
    "@scola/auth-common": "^2.0.0",
    "@scola/error": "^4.2.0",
    "bcrypt": "^1.0.1",
    "jsonwebtoken": "^7.2.1",
    "useragent": "^2.1.9"
  },
  "devDependencies": {
    "rollup": "^0.37.0",
    "rollup-plugin-buble": "^0.15.0",
    "uglify-js": "^2.7.5"
  },
  "scripts": {
    "build": "npm run cleanup && npm run rollup",
    "build:min": "npm run build && npm run min",
    "cleanup": "rm -rf dist",
    "min": "uglifyjs dist/auth-server.js -m -o dist/auth-server.min.js",
    "prepublish": "npm run build:min",
    "rollup": "rollup -c"
  }
}
